logging:
  level: debug
host:
  daemon: "unix:///var/run/docker.sock"
check:
  retries: 3  # days
  interval: 5  # seconds
rules:
  rule1:
    filter:
      All: true
    condition: "ls"
    action: "a1"